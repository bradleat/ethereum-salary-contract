{
  /*
   * Possible values:
   * - the name of a built-in config
   * - the name of an NPM module which has a "main" file that exports a config object
   * - a relative path to a JSON file
   */
  "extends": "tslint:recommended",
  "rulesDirectory": [
    "node_modules/tslint-eslint-rules/dist/rules"
  ],
  "rules": {
    /*
     * Any rules specified here will override those from the base config we are extending.
     */
     "curly": true,
     "quotemark": [
       false
     ],
     "whitespace":[
       true, "check-decl", "check-operator", "check-module", "check-separator", "check-type", "check-typecast"
     ],
     "one-line": [
       false
      ],
     "object-literal-sort-keys": false,
     "ordered-imports": [
       false
     ],
     "trailing-comma": [
       false
     ],
     "member-ordering": [
       false
     ],
     "max-classes-per-file": [
       true, 5
     ],
     "triple-equals": [
       true,
       "allow-null-check",
       "allow-undefined-check"
     ],
     "no-console": [
       false
     ]
  },
  "jsRules": {
    /*
     * Any rules specified here will override those from the base config we are extending.
     */
     "curly": true,
     "quotemark": [
       false
     ],
     "whitespace":[
       true, "check-decl", "check-operator", "check-module", "check-separator", "check-type", "check-typecast"
     ],
     "one-line": [
       false
      ],
     "object-literal-sort-keys": false,
     "ordered-imports": [
       false
     ],
     "trailing-comma": [
       false
     ]
  }
}
